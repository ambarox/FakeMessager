package com.fakeMessager.controller;

/**
 * The interface On headline selected listener.
 */
public interface OnNumberSelectedListener {

    /**
     * On number selected.
     *
     * @param position the position
     */
    void onNumberSelected(int position);
}
